import { HtmlCleanupPipe } from './html-cleanup.pipe';

describe('HtmlCleanupPipe', () => {
  it('create an instance', () => {
    const pipe = new HtmlCleanupPipe();
    expect(pipe).toBeTruthy();
  });
});
