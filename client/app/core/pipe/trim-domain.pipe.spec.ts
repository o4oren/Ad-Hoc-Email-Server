import { TrimDomainPipe } from './trim-domain.pipe';

describe('TrimDomainPipe', () => {
  it('create an instance', () => {
    const pipe = new TrimDomainPipe();
    expect(pipe).toBeTruthy();
  });
});
